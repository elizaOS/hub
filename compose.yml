networks:
  forgejo:
    external: false

services:
  server:
    image: codeberg.org/forgejo/forgejo:15
    container_name: eliza-forgejo-local
    environment:
      USER_UID: "1000"
      USER_GID: "1000"
      FORGEJO____APP_NAME: "Eliza Hub"
      FORGEJO__server__DOMAIN: "localhost"
      FORGEJO__server__HTTP_PORT: "3000"
      FORGEJO__server__ROOT_URL: "http://localhost:3000/"
      FORGEJO__server__SSH_DOMAIN: "localhost"
      FORGEJO__server__SSH_PORT: "2222"
      FORGEJO__server__SSH_LISTEN_PORT: "22"
      FORGEJO__security__INSTALL_LOCK: "true"
      FORGEJO__service__DISABLE_REGISTRATION: "false"
      # New accounts come from the Eliza Cloud OIDC source only; the local
      # signup form stops creating them. The password form itself stays until
      # the provider is production-backed, because it is the recovery path if
      # the issuer is unreachable — see docs/eliza-cloud-sso-plan.md.
      FORGEJO__service__ALLOW_ONLY_EXTERNAL_REGISTRATION: "true"
      # Without these the OIDC source authenticates but creates no account, so
      # a first-time sign-in dead-ends. USERNAME must stay `nickname`: Forgejo
      # rejects `preferred_username` as a username source and silently falls
      # back to nickname anyway. See docs/eliza-cloud-sso-plan.md.
      FORGEJO__oauth2_client__ENABLE_AUTO_REGISTRATION: "true"
      FORGEJO__oauth2_client__REGISTER_EMAIL_CONFIRM: "false"
      FORGEJO__oauth2_client__USERNAME: "nickname"
      FORGEJO__oauth2_client__ACCOUNT_LINKING: "login"
      # Eliza Cloud is the only way in: the username/password form is removed
      # from the sign-in page entirely. Recovery, if the issuer is ever
      # unreachable, is to set this back to "true" and restart — the local
      # credential is still valid, only the form is withdrawn.
      FORGEJO__service__ENABLE_INTERNAL_SIGNIN: "false"
      FORGEJO__service__REQUIRE_SIGNIN_VIEW: "false"
      FORGEJO__actions__ENABLED: "true"
      FORGEJO__actions__DEFAULT_ACTIONS_URL: "https://data.forgejo.org"
      FORGEJO__ui__THEMES: "forgejo-auto,forgejo-light,forgejo-dark,eliza,eliza-light"
      FORGEJO__ui__DEFAULT_THEME: "eliza"
      # Link previews and search results otherwise describe upstream Forgejo
      # rather than this instance; og:description is derived from these.
      # The section is [ui.meta], not [ui] — values under [ui] are silently
      # ignored and the page keeps serving the upstream defaults.
      FORGEJO__ui_0X2E_meta__AUTHOR: "Eliza Hub"
      FORGEJO__ui_0X2E_meta__DESCRIPTION: "An open git forge for agent-built software. Host your repositories, run CI, and let the Eliza Army finish your issues."
      FORGEJO__ui_0X2E_meta__KEYWORDS: "eliza,elizaos,git,forge,agents,open source"
      # Large pilot repositories (elizaOS/eliza) exceed the 600s default
      # migration timeout. Matches templates/forgejo/app.staging.example.ini.
      FORGEJO__git_0X2E_timeout__MIGRATE: "7200"
    restart: unless-stopped
    networks:
      - forgejo
    volumes:
      - ./forgejo:/data
      - ./custom/public:/data/gitea/public:ro
      - ./custom/templates:/data/gitea/templates:ro
      - /etc/localtime:/etc/localtime:ro
    ports:
      - "127.0.0.1:3000:3000"
      - "127.0.0.1:2222:22"
